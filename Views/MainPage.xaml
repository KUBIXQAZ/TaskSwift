<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TaskSwift.Views.MainPage">
    <ScrollView BackgroundColor="#121212">
        <StackLayout x:Name="TasksContainer" 
                     Padding="0,10,0,0">
            <!--<Frame BackgroundColor="#373737"
                   CornerRadius="15"
                   Margin="8,3,8,0"
                   BorderColor="Transparent"
                   HeightRequest="80"
                   Padding="0">
                <AbsoluteLayout>
                    <Label Text="polski" FontSize="20" Margin="40,10,0,0"/>
                    <Label Text="2 Days Left" FontSize="20" Margin="40,40,0,0"/>
                    <Button BackgroundColor="Transparent" AbsoluteLayout.LayoutFlags="All" AbsoluteLayout.LayoutBounds="0,0,1,1" />
                    <RadioButton Margin="6,5,0,0"/>
                </AbsoluteLayout>
            </Frame>-->
        </StackLayout>
    </ScrollView>
</ContentPage>